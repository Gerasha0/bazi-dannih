-- ============================================================
-- 06_add_table_occupied.sql
-- Додає прапор фізичної зайнятості столика.
-- Виконати один раз після оновлення застосунку.
-- ============================================================

ALTER TABLE tables
    ADD COLUMN is_occupied TINYINT(1) NOT NULL DEFAULT 0
    COMMENT 'Фізична зайнятість: 1 = люди сидять, 0 = вільно';
